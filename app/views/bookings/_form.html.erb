<br><br><br><br>

<%= simple_form_for([@user, booking]) do |form| %>
  <% if booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
        <% booking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
    <% else %>
      <%= simple_form_for [@user, @booking] do |f| %>
      <%= f.input :date %>
      <%= f.input :set_length %>
      <%= f.input :hourly_rate %>
      <%= f.input :music_genre %>
      <%= f.input :venue %>
      <%= f.input :equipment_type %>
      <%= f.input :description %>
      <%= f.button :submit, "Update", class: "btn-submit"%>
    <% end %>
  <% end %>

<% end %>
