<h1>BiteJockey Magic Playlist Organiser</h1>

<div class="container">
  <% @playlists.each do |playlist| %>
    <h3><%=link_to playlist.name, playlist_path(playlist: playlist) %></h3>
    <% end %>
</div>
